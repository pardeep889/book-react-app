import React from 'react';
import EditBookContainer  from "../../containers/Admin/editBook_container";
const Edit = (props) => {
    return(
        <EditBookContainer {...props}/>
    )
}

export default Edit;