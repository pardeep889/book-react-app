import React from 'react';
import UserPostContainer from '../../containers/Admin/userPosts_container';

const UserPosts = (props) => {
    return(
        <UserPostContainer {...props}/>
    )
}
export default UserPosts;