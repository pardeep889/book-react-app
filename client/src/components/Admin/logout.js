import React from 'react';
import LogoutContainer from '../../containers/Admin/logout_container';


const Logout = (props) => {
    return (
        <LogoutContainer {...props}/>
    )
}

export default Logout;