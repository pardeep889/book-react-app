import React from 'react';
import LoginContainer from '../../containers/Admin/login_container';
const Login = (props) => {
    return(
        <LoginContainer {...props}/>
    )
}

export default Login;